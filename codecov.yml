comment:
  layout: "reach, diff, flags, files"
  behaviour: default
  require_changes: false
  require_head: false    # default yes
  require_base: false     # default yes
ignore:
  - "src/tests/*”
coverage:
  status:
    default_rules:
      flag_coverage_not_uploaded_behavior: exclude
    project: false
    patch: false